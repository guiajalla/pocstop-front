import React from 'react'
import { css } from '@emotion/react'


const footerStyles = css`
    display: flex;
    `

export const Footer = () => {
    return (
        <div css={footerStyles}></div>
    )
}